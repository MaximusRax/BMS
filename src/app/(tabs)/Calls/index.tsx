import { StyleSheet, Text, View } from "react-native";

const index = () => {
  return (
    <View>
      <Text>Calls</Text>
    </View>
  );
};

export default index;

const styles = StyleSheet.create({});
